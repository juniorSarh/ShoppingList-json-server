{
  "name": "shopping-list-api",
  "version": "1.0.0",
  "description": "Shopping List JSON Server API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "uuid": "^9.0.1"
  }
}
