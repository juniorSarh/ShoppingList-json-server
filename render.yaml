services:
  - type: web
    name: shopping-list-api
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /
